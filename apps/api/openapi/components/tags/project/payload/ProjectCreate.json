{
  "type": "object",
  "description": "Payload for approving an application. Includes the application UUID and any updates to its definition.",
  "required": [
    "uuidApplication",
    "title",
    "shortDescription",
    "description",
    "deadline",
    "projectType"
  ],
  "properties": {
    "uuidApplication": {
      "type": "string",
      "format": "uuid",
      "description": "The UUID of the application to approve.",
      "example": "123e4567-e89b-12d3-a456-426614174000"
    },
    "title": {
      "type": "string",
      "description": "The main title of the project.",
      "example": "Sistema de Gestión de Residuos"
    },
    "shortDescription": {
      "type": "string",
      "description": "A brief, one-sentence summary.",
      "example": "Una app para optimizar la recolección de basura."
    },
    "description": {
      "type": "string",
      "description": "Detailed description covering the problem and solution.",
      "example": "Este proyecto busca resolver el problema de rutas ineficientes..."
    },
    "deadline": {
      "type": "string",
      "format": "date",
      "description": "The application deadline (YYYY-MM-DD).",
      "example": "2025-12-31"
    },
    "projectType": {
      "description": "Associated project type ID(s). Accepts a single ID or an array of IDs. **Must result in a single selection for approval.**",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": 1
    },
    "problemType": {
      "description": "Associated problem type ID(s). Accepts a single ID or an array of IDs.",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": [1, 2]
    },
    "faculty": {
      "description": "Associated faculty ID(s). Accepts a single ID or an array of IDs.",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": 5
    },
    "problemTypeOther": {
      "type": "string",
      "description": "User-defined problem type description if 'other' is selected.",
      "nullable": true,
      "example": "Innovación Educativa"
    }
  }
}
