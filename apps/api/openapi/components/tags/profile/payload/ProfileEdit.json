{
  "type": "object",
  "description": "Payload for updating user and outsider profile information.",
  "properties": {
    "firstName": {
      "type": "string",
      "example": "John"
    },
    "middleName": {
      "type": "string",
      "example": "Fitzgerald"
    },
    "lastName": {
      "type": "string",
      "example": "Doe"
    },
    "phoneNumber": {
      "type": "string",
      "description": "User's contact phone number.\n - Required _if_ user have **outsider** role.",
      "example": "+523141234567"
    },
    "location": {
      "type": "string",
      "description": "User's location or address.\n - Required _if_ user have **outsider** role.",
      "example": "Manzanillo, Colima, Mexico"
    },
    "organizationName": {
      "type": "string",
      "description": "Name of the outsider's organization.\n - Required _if_ user have **outsider** role.",
      "example": "Example Corp."
    }
  },
  "required": ["firstName", "middleName", "lastName"]
}
