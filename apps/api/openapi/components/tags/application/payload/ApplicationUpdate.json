{
  "type": "object",
  "required": ["title", "shortDescription", "description", "deadline"],
  "properties": {
    "title": {
      "type": "string",
      "description": "The main title of the application.",
      "example": "Sistema de Gestión de Residuos"
    },
    "shortDescription": {
      "type": "string",
      "description": "A brief, one-sentence summary.",
      "example": "Una app para optimizar la recolección de basura."
    },
    "description": {
      "type": "string",
      "description": "A detailed description of the problem and solution.",
      "example": "Este proyecto busca resolver el problema de rutas ineficientes..."
    },
    "deadline": {
      "type": "string",
      "format": "date",
      "description": "The application deadline (YYYY-MM-DD).",
      "example": "2025-12-31"
    },
    "projectType": {
      "description": "Associated project type ID(s). Accepts a single ID or an array of IDs.",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": 1
    },
    "problemType": {
      "description": "Associated problem type ID(s). Accepts a single ID or an array of IDs.",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": [1, 2]
    },
    "faculty": {
      "description": "Associated faculty ID(s). Accepts a single ID or an array of IDs.",
      "oneOf": [
        { "type": "string" },
        { "type": "integer" },
        {
          "type": "array",
          "items": { "oneOf": [{ "type": "string" }, { "type": "integer" }] }
        }
      ],
      "example": 5
    },
    "problemTypeOther": {
      "type": "string",
      "description": "Custom problem type definition if 'other' is selected.",
      "example": "Innovación Educativa"
    }
  }
}
